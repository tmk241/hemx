[package]
name = "hemx-sync"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
description = "Durable command, event, and projection primitives for Hemx"
keywords = ["hypermedia", "sync", "events", "web"]
categories = ["web-programming"]

[dependencies]
hemx-core = { version = "0.1.0", path = "../hemx-core" }
hemx-sync-macros = { version = "0.1.0", path = "../hemx-sync-macros" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
